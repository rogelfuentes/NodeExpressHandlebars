<h1>Pizzas!</h1>

<h2>Pizzas that are ready to eat!</h2>

<ul>
  {{#each pizzas}}
    {{#unless devoured}}
      {{> pizzas/pizza-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Pizzas that are devoured!</h2>

<ul>
  {{#each pizzas}}
    {{#if devoured}}
      {{> pizzas/pizza-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Pizza</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Pizza Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> Devoured!<br>
    <input type="radio" name="devoured" value="0"> Meal
  </div>

  <button type="submit">Add Pizza</button>
</form>
